import dotenv from 'dotenv'; dotenv.config(); import { sendTemplatedNotification } from './src/modules/messageTemplates/messageTemplate.service.js'; import { pool } from './src/config/db.js'; async function run() { try { console.log('Starting...'); const res = await sendTemplatedNotification({ eventKey: 'MEMBER_CREATED', tenantId: 2, receiverId: 2, receiverRole: 'Member', receiverEmail: 'test@test.com' }); console.log(res); } catch(err) { console.error(err); } finally { pool.end(); } } run();
